{
    "name": "phishdetect",
    "version": "5.0.1",
    "description": "",
    "main": "",
    "scripts": {
        "deps": "npm install",
        "stage": "mkdir dist || echo ''",
        "bundle": "browserify -o dist/gmail.js js/gmail.js && browserify -o dist/domains.js js/domains.js && browserify -o dist/gui.js js/gui.js",
        "build": "npm run stage && npm run bundle"
    },
    "author": "Claudio Guarnieri",
    "license": "GPL-3.0",
    "repository": {
        "type": "git",
        "url": "https://github.com/phishdetect/phishdetect-extension.git"
    },
    "dependencies": {
        "gmail-js": "^0.9.1",
        "npm": "^6.10.1",
        "tldts": "^3.1.2",
        "vex-dialog": "^1.1.0",
        "vex-js": "^4.1.0"
    },
    "devDependencies": {
        "browserify": "latest"
    }
}
